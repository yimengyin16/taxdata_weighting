
# See: https://gist.github.com/jennybc/362f52446fe1ebc4c49f

RPROJ <- list(PROJHOME = normalizePath(getwd()))
attach(RPROJ)

cat("sourcing Project-specific .Rprofile\n\n")

cat('retrieve the top-level Project directory at any time with PROJHOME or via get("PROJHOME", "RPROJ"):\n',
    get("PROJHOME", "RPROJ"), "\n\n")

cat('use PROJHOME as in this example:
  source(file.path(PROJHOME, "r/includes", "libraries.r"))')

rm(RPROJ)

